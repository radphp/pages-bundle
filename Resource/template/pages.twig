{% extends getMasterTwig() %}

{% set title = page.title %}

{% block base %}{{ generateUrl(['pages'], false, true, false) }}/{% endblock %}

{% block content %}
<div class="jumbotron text-center">
    <h1>{{ page.title }}</h1>

    {{ page.body|raw }}
</div>
{% endblock %}
